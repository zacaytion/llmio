[maxbodylength]
disabled = false
maxBodyLength = 20

[minphony]
# Our Makefile uses .DEFAULT_GOAL=help and has all required phony targets
# (all, clean, test) properly declared. Checkmake's parsing seems buggy.
disabled = true

[phonydeclared]
# Disabled because we have file targets (bin/server, bin/migrate) that are
# correctly NOT phony, with corresponding phony aliases (build-server, etc.)
disabled = true

[phonydeclaliased]
disabled = true
